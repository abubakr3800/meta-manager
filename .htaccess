# Protect config/includes/sql/.env/.git. Do not deny public/.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteRule ^(config|includes|sql)(/|$) - [F,L]
  RewriteRule ^(\.env|\.git) - [F,L]
  RewriteRule ^$ public/login.php [L]
</IfModule>
